package api.lang.etc;

import java.math.MathContext;

public class Test01 {
public static void main(String [] ages) {
	// 생성자가 없는 클래스
	//클래스는 객체 생성이 주 목적이다.
	// 생성자가 없다는 것은 생성자가 private 이란 말이다.
	// 생성자가 없다는것은 객체를 만들수 없다는 의미
	
	//Math a  =new Math(); 이런것이 안된다.
	
	// 1.객체를 아예 못만들게 하려는 목적
	// 모든 구성요소가 전부다 Static 이다
	// 2.객체를 만들어주는 메소드를 제공하려는 목적(통제)
	// 구성요소 중 static 아닌것이 있는 경우 
	
}
}
